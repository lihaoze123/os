[package]
name = "os"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "os"
path = "src/main.rs"
test = false
bench = false

[dependencies]
log = "0.4.33"
owo-colors = { version = "4", default-features = false }
sbi-rt = { version = "0.0.2", features = ["legacy"] }
